- name: Instalación de Nginx.
  apt:
    name: nginx

- name: Configuración del virtualhost.
  template: >
    src=etc/nginx/conf.d/wordpress
    dest=/etc/nginx/conf.d/wordpress
    owner=root
    group=root
    mode=644
  notify: restart-nginx
